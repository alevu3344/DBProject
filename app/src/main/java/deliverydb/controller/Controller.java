package deliverydb.controller;

/**
 * The Controller interface defines the contract for controllers in the DeliveryDB application.
 * It ensures that all controllers implement a method to display their respective views.
 */
public interface Controller {

    /**
     * Displays the view associated with the implementing controller.
     */
    public void show();

}
